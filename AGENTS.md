# AGENTS.md

本 repo 是一個可安裝的稽核 Skill，不是應用程式。沒有建置步驟、沒有依賴、沒有測試。

## 執行稽核

主要指示在 [`SKILL.md`](SKILL.md)。要對某個專案做安全稽核時，完整讀取該檔並依其流程執行，再按需載入 `references/` 下對應維度的檢查清單。

不要一次載入全部 references——一次一個維度。

## 修改本 repo 時

- `SKILL.md` 只放流程、嚴重度模型、硬規則。具體檢查項目一律放 `references/`。
- 每個 references 檔案結尾都必須有「常見誤判，不要報」段落。控制誤報率是這個 Skill 能不能被持續使用的關鍵。
- 每個檢查項目要能寫出具體觸發情境（誰送什麼、拿到什麼）。寫不出來的項目不要加。
- 保持繁體中文，技術名詞維持英文。
